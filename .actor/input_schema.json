{
  "title": "OpenTable Reviews Scraper",
  "description": "Scrape reviews from OpenTable restaurants. Provide a search URL to find restaurants or direct restaurant URLs.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of OpenTable URLs (Search results or specific Restaurant pages) to scrape reviews from.",
      "editor": "requestListSources",
      "prefill": [
        {
          "url": "https://www.opentable.com/r/bestia-los-angeles"
        }
      ]
    },
    "results_wanted": {
      "title": "Maximum number of reviews",
      "type": "integer",
      "description": "The maximum number of reviews to collect per run.",
      "minimum": 1,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy for reliable scraping. Residential proxies are recommended.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": [
    "proxyConfiguration"
  ],
  "additionalProperties": false
}